# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

# You need personal access token on github for GitHub Metadata plugin in local preview.
# To fix the warning, create .env file with $JEKYLL_GITHUB_TOKEN you generated with repo/public_repo scope(minimum).
# For further information, see
# http://jekyll.github.io/github-metadata/authentication/
# https://docs.github.com/en/github/authenticating-to-github/keeping-your-account-and-data-secure/creating-a-personal-access-token
gem "dotenv" 
gem "github-pages", group: :jekyll_plugins
